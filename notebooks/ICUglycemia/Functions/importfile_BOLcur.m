function BolusesCUR = importfile_BOLcur(workbookFile, sheetName, dataLines)
%IMPORTFILE Import data from a spreadsheet
%  BOLUSESCUR = IMPORTFILE(FILE) reads data from the first worksheet in
%  the Microsoft Excel spreadsheet file named FILE.  Returns the data as
%  a table.
%
%  BOLUSESCUR = IMPORTFILE(FILE, SHEET) reads from the specified
%  worksheet.
%
%  BOLUSESCUR = IMPORTFILE(FILE, SHEET, DATALINES) reads from the
%  specified worksheet for the specified row interval(s). Specify
%  DATALINES as a positive scalar integer or a N-by-2 array of positive
%  scalar integers for dis-contiguous row intervals.
%
%  Example:
%  BolusesCUR = importfile("BolusesCUR.xlsx", "BolusesCUR", [2, 88465]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 14-Apr-2020 19:00:09

%% Input handling

% If no sheet is specified, read first sheet
if nargin == 1 || isempty(sheetName)
    sheetName = 1;
end

% If row start and end points are not specified, define defaults
if nargin <= 2
    dataLines = [2, 88465];
end

%% Setup the Import Options and import the data
opts = spreadsheetImportOptions("NumVariables", 22);

% Specify sheet and range
opts.Sheet = sheetName;
opts.DataRange = "A" + dataLines(1, 1) + ":V" + dataLines(1, 2);

% Specify column names and types
opts.VariableNames = ["SUBJECT_ID", "HADM_ID", "ICUSTAY_ID", "ICU_ADMISSIONTIME", "ICU_DISCHARGETIME", "Var6", "Var7", "TIMER", "STARTTIME", "Var10", "Var11", "INPUT", "Var13", "Var14", "Var15", "Var16", "Var17", "Var18", "GLC_AL", "GLCTIMER_AL", "GLCSOURCE_AL", "RULE"];
opts.SelectedVariableNames = ["SUBJECT_ID", "HADM_ID", "ICUSTAY_ID", "ICU_ADMISSIONTIME", "ICU_DISCHARGETIME", "TIMER", "STARTTIME", "INPUT", "GLC_AL", "GLCTIMER_AL", "GLCSOURCE_AL", "RULE"];
opts.VariableTypes = ["double", "double", "double", "datetime", "datetime", "char", "char", "datetime", "datetime", "char", "char", "double", "char", "char", "char", "char", "char", "char", "double", "datetime", "categorical", "double"];

% Specify variable properties
opts = setvaropts(opts, ["Var6", "Var7", "Var10", "Var11", "Var13", "Var14", "Var15", "Var16", "Var17", "Var18"], "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["Var6", "Var7", "Var10", "Var11", "Var13", "Var14", "Var15", "Var16", "Var17", "Var18", "GLCSOURCE_AL"], "EmptyFieldRule", "auto");
opts = setvaropts(opts, "ICU_ADMISSIONTIME", "InputFormat", "");
opts = setvaropts(opts, "ICU_DISCHARGETIME", "InputFormat", "");
opts = setvaropts(opts, "TIMER", "InputFormat", "");
opts = setvaropts(opts, "STARTTIME", "InputFormat", "");
opts = setvaropts(opts, "GLCTIMER_AL", "InputFormat", "");

% Import the data
BolusesCUR = readtable(workbookFile, opts, "UseExcel", false);

for idx = 2:size(dataLines, 1)
    opts.DataRange = "A" + dataLines(idx, 1) + ":V" + dataLines(idx, 2);
    tb = readtable(workbookFile, opts, "UseExcel", false);
    BolusesCUR = [BolusesCUR; tb]; %#ok<AGROW>
end

end